#!/usr/bin/env zsh
set -e

# make temp dir and cd temp
CURRENT_DIR=$(pwd)

cd ~/workspace
if [ -d "temp" ];then
    rm -rf temp
fi
mkdir -p temp

cd temp

zsh

if [ -d "temp" ];then
    rm -rf temp
fi

cd $CURRENT_DIR