# Git Rules

## when to use

- branch、commit、diff、status、push、merge request など Git 操作を行うとき。
- ユーザー変更を含む作業ツリーで変更範囲を判断するとき。

## Git運用

- ユーザー指示なし commit禁止
- branch作成 ユーザー指示優先
- ブランチ/コミット作成時は下記スキルを利用
  - `create-branch`
  - `commit-current-changes`
