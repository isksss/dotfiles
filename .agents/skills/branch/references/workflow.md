# branch workflow

- 現在の branch と作業ツリーを確認する。
- 作業目的を2、3語の短い英語またはローマ字にする。
- 形式は `{gitflow}/{summary}_{yyyymmdd}` を基本にする。
- gitflow は feature、bugfix、hotfix、release から選ぶ。
- 既存 branch と重複する場合は要約を調整する。
- `git checkout -b` で作成する。
