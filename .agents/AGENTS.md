# Global AGENTS.md

日本語で簡潔に回答し、事実・既存規約・最小変更を優先する。詳細は `.agents/rules/`、実作業は `research` `implement` `review` `commit` `branch` `merge-request` を使う。

## 追記事項

- 開発サーバやDockerなどを起動する際はskill`herdr`を利用し、別パネルを作成し、そこで起動してください。
