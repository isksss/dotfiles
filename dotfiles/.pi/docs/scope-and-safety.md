# スコープと安全性

- 依頼を満たす最小限の変更を優先し、既存の構造と実装を再利用する。
- 要求外のリファクタ、最適化、将来を見越した抽象化、依存関係の追加をしない。
- 編集前に Git 状態と対象ファイルを確認し、ユーザーの既存差分や未追跡ファイルを保持する。
- branch 作成、commit、push、PR/MR 作成はユーザーが明示的に依頼した場合だけ実行する。
- データ削除、履歴の書き換え、既存作業の破棄など、破壊的な操作は対象と影響を示して確認する。
- 秘密情報や認証情報を出力・記録・コミットしない。環境変数や認証ファイルを一括表示しない。
